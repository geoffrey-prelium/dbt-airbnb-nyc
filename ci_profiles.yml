immorent:
  target: dev
  outputs:
    dev:
      type: bigquery
      method: service-account-json  # On utilise le robot, pas OAuth
      project: cours-dbt-479309     # Ton ID projet (Vérifie que c'est le bon)
      dataset: dbt_geoffrey_airbnb  # Ton dataset
      threads: 4
      location: US
      priority: interactive
      # C'est ici que la magie opère : on récupère la clé depuis une variable d'environnement
      keyfile_json: "{{ env_var('DBT_GOOGLE_BIGQUERY_KEYFILE_JSON') }}"